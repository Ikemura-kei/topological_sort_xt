# Topological sort stuff for XT

## Files

* __find_max_anti_chain.py__: contains the function that gives the maximum anti-chain size, which is also the size of the largest S in Kahn's algorithm.
* __test_cases.py__: contains the test cases I prepared, you can run it as a reference. The usage is illustrated in the file.